# Guardian 部署环境变量样本
# 复制为 deploy/.env 后修改

# 控制台访问口令（强烈建议改成强密钥）
ACCESS_TOKEN=guardian-demo-2026

# 对外域名；localhost 时 Caddy 自签本地证书供本机验证
GUARDIAN_DOMAIN=localhost

# Postgres
POSTGRES_USER=guardian
POSTGRES_PASSWORD=guardian
POSTGRES_DB=guardian

# 死人开关倒计时（分钟）
TRIAL_MINUTES=5

# 人话兜底（留空则只用静态目录）
ANTHROPIC_API_KEY=

# 控制台公网访问基地址（拼装一键加机脚本使用，裸IP部署时必须填 http://<ip>:18081）
CONSOLE_BASE_URL=http://localhost
